<h2><%= author.name %></h2>
<div class="btn-row">
    <a class="btn btn-primary" href="/authors/<%= author.id%>/edit">Edit</a>
    <%- include('../partials/deleteForm.ejs', {url: `/authors/${author.id}`}) %>
</div>

<div>
    <% if(booksByAuthor.length > 0){%>
        <h2 class="page-header">Books by <%= author.name %></h2>
        <%- include('../partials/bookSession.ejs', {books : booksByAuthor}) %>
    <% } %>
</div>
